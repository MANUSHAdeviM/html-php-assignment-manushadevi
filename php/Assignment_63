<?php
function sumEvenNumbers(array $numbers) {
    $sum = 0;
    foreach ($numbers as $num) {
        if ($num % 2 === 0) {  // Check if the number is even
            $sum += $num;
        }
    }
    return $sum;
}

// Example usage:
$array = [1, 2, 3, 4, 5, 6];
echo "Sum of even numbers: " . sumEvenNumbers($array);
?>

OUTPUT:

Sum of even numbers: 12
